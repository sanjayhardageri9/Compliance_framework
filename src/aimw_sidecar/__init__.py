"""aimw FastAPI sidecar control plane (Phase 1 skeleton).

Wraps GovernancePipeline over HTTP. Requires optional extra ``aimw[sidecar]``.
NOT production-ready — route bodies are stubs.
"""

__version__ = "0.0.0-skeleton"
