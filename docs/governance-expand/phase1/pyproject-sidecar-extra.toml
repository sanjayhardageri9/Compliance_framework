# Snippet to merge into the main aimw pyproject.toml

[project.optional-dependencies]
sidecar = [
    "fastapi>=0.110",
    "uvicorn>=0.27",
    "httpx>=0.27",
]

# Optional: also keep prometheus-client if ops wants a full registry later.
# prometheus-client>=0.20 is NOT required for Phase 1 — metrics.render_prometheus()
# emits text exposition from in-process counters.

# Entrypoint sketch (when merged):
# [project.scripts]
# aimw-sidecar = "aimw_sidecar.app:main"

# Hatch must package the sidecar too:
# [tool.hatch.build.targets.wheel]
# packages = ["src/aimw", "src/aimw_sidecar"]
